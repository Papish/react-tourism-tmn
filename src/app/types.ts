export type PostList = {
  id: string;
  slug: string;
  shortTitle: any;
};

export type Post = {
  content: string;
  shortTitle: string;
  title: string;
  date: string;
  image: string;
  description: string;
  slug: string;
};
